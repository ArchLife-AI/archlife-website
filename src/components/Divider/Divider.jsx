export function Divider() {
  return (
    <div
      style={{
        width: "100%",
        height: "1px",
        background:
          "linear-gradient(to right, transparent, rgba(156,163,175,0.15), transparent)",
        margin: "0 auto",
      }}
    />
  );
}
